// getStatesOwned.cpp - Returns number of states owned

#include "../Player.hpp"

int Player::getStatesOwned() {

return stateClasses[0] + stateClasses[1] + stateClasses[2] + stateClasses[3] + stateClasses[4] + stateClasses[5];

}
